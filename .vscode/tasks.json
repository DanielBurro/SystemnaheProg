{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run RISC-V Assembly with RARS",
            "type": "shell",
            "command": "java",
            "args": [
                "-jar",
                "${workspaceFolder}/rars1_6.jar",
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "isBackground": false,
            "problemMatcher": [],
            "group": "build"
        }
    ]
}